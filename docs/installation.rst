============
Installation
============

At the command line::

    $ easy_install django-blarg

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-blarg
    $ pip install django-blarg